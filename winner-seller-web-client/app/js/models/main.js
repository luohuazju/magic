define([
  'underscore',
  'backbone'
], function(_, Backbone) {
  var mainModel = Backbone.Model.extend({
    defaults: {
		
    },
    initialize: function(){
    }
    
  });
  return mainModel;

});
