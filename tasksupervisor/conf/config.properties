##############################################
# \u6570\u636e\u5e93\u914d\u7f6e
##############################################
# mysql database below
#jdbc.driverClassName=com.mysql.jdbc.Driver
#jdbc.url=jdbc:mysql://localhost:3306/quartz?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true
#jdbc.username=root
#jdbc.password=kaishi
# oracle database below
jdbc.driverClassName=oracle.jdbc.driver.OracleDriver
jdbc.url=jdbc:oracle:thin:@10.206.20.5:1521:ccdev
jdbc.username=cc
jdbc.password=cc

# Time to wait for an open connection before timing out(in milliseconds)
cpool.checkoutTimeout=5000
# Connection pool size
cpool.minPoolSize=10
cpool.maxPoolSize=25
# How long to keep unused connections around(in seconds)
# Note: MySQL times out idle connections after 8 hours(28,800 seconds)
# so ensure this value is below MySQL idle timeout
cpool.maxIdleTime=7200
# Acquiring new connections is slow, so eagerly retrieve extra connections
# when current pool size is reached
cpool.acquireIncrement=5
cpool.autoCommitOnClose=true

################################################
# velocity path
################################################
velocity.file.path=file://e:/work/tasksupervisor
